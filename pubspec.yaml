name: swipe_image_gallery
description: A scrollable, dismissable by swiping, zoomable, rotatable image gallery on which you can add a dynamic overlay.
version: 0.0.3
homepage: https://github.com/dbilgin/swipe_image_gallery

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
